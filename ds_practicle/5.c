#include <stdio.h>

int main() {
  char str[100];
  int i;

  printf("Enter a string: ");
  gets(str);

  // find length of string
  for (i = 0; str[i] != '\0'; i++) {
    
  }

  printf("Length of the string: %d", i);

  return 0;
}
